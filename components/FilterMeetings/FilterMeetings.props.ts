import { DetailedHTMLProps, HTMLAttributes, ReactNode} from 'react';

export interface FilterMeetingsProps extends DetailedHTMLProps<HTMLAttributes<HTMLElement>, HTMLElement> {
	// children: ReactNode;
}